{% extends 'base.html.twig' %}

{% block title %}New Score{% endblock %}

{% block body %}
    <h1>Create new Score for Gradebook</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}
{% endblock %}
