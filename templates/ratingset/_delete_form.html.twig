<form method="post" action="{{ path('app_ratingset_delete', {'id': ratingset.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ratingset.id) }}">
    <button class="btn">Delete</button>
</form>
