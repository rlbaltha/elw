{% extends 'base.html.twig' %}

{% block title %}Term New - {{ parent() }}{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-body">
            <h1 class="card-title">New Term</h1>
            {{ include('term/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
