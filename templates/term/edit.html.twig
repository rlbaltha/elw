{% extends 'base.html.twig' %}

{% block title %}Term Edit - {{ parent() }}{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-body">
            <h1 class="card-title">Edit Term</h1>
            {{ include('term/_form.html.twig', {'button_label': 'Update'}) }}

            {{ include('term/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
