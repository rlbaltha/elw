{% extends 'base.html.twig' %}

{% block title %}Markup Set Edit- {{ parent() }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link type="text/css" rel="stylesheet" href="{{ asset('css/ckeditor_document_v2.css')}}">
    <link type="text/css" rel="stylesheet" href="{{ asset('style.css')}}">
    <link type="text/css" rel="stylesheet" href="{{ asset('ckeditor5/ckeditor5.css')}}">
{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-body">
            <h1 class="card-title">Edit Markup Set</h1>
            {{ include('markupset/_form.html.twig', {'button_label': 'Update'}) }}

            {{ include('markupset/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        $(document).ready(function () {
            {% if not is_granted('ROLE_ADMIN') %}
            $('#markupset_level').hide();
            $('#markupset_level').prev().hide();
            {% endif %}
        });
    </script>
    <script type="importmap">
        {
            "imports": {
                "ckeditor5": "{{ asset('ckeditor5/ckeditor5.js') }}",
				"ckeditor5/": "./ckeditor5/"
			}
		}
    </script>
    <script type="module" src="{{ asset('js/markupset_editor_init.js') }}"></script>
{% endblock %}