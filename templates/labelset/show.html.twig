{% extends 'base.html.twig' %}

{% block title %}Labelset{% endblock %}

{% block body %}
    <h1>Labelset</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ labelset.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ labelset.name }}</td>
            </tr>
            <tr>
                <th>Level</th>
                <td>{{ labelset.level }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('labelset_index') }}">back to list</a>

    <a href="{{ path('labelset_edit', {'id': labelset.id}) }}">edit</a>

    {{ include('labelset/_delete_form.html.twig') }}
{% endblock %}
