{% extends 'base.html.twig' %}

{% block title %}Edit Labelset{% endblock %}

{% block body %}
    <h1>Edit Labelset</h1>

    {{ include('labelset/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('labelset_index') }}">back to list</a>

    {{ include('labelset/_delete_form.html.twig') }}
{% endblock %}
