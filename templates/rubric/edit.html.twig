{% extends 'base.html.twig' %}

{% block title %}Rubric Edit - {{ parent() }}{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-body">
            <h1 class="card-title">Edit Rubric</h1>
            {{ include('rubric/_form.html.twig', {'button_label': 'Update'}) }}

            {{ include('rubric/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
