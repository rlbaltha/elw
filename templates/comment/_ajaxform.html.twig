{{ form_start(form, {'attr': {'class': 'd-inline', 'id':'ajax'}}) }}
    {{ form_widget(form) }}
{{ form_end(form) }}
<a class="btn btn-primary btn-sm ajax_submit" href="#">Submit</a>

<a class="btn btn-primary btn-sm float-end ajax_cancel" href="#">Cancel</a>